<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Публичный URL лендинга (для подсказок в админке)
    |--------------------------------------------------------------------------
    */

    'site_url' => env('LANDING_SITE_URL', env('APP_URL', 'https://otuken.example')),

    /*
    |--------------------------------------------------------------------------
    | Fallback: e-mail получателей заявок (если не задано в админке)
    |--------------------------------------------------------------------------
    |
    | Основной способ — «Контент лендинга → Заявки с карты» в админке.
    | Здесь можно указать один адрес или несколько через запятую.
    |
    */

    'leads_manager_email' => env('LEADS_MANAGER_EMAIL'),

];
